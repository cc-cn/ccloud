假定pytorch已经安装完成

git clone https://github.com/amdegroot/ssd.pytorch.git

数据之前复现caffe-ssd的时候下载过，可以直接用。按序解压到~/data/。

下载模型（假定原始路径在ssd.pytorch下）
mkdir weights
cd weights
wget https://s3.amazonaws.com/amdegroot-models/vgg16_reducedfc.pth（如果下载慢的话，可以先ssh到我买的vps下，下载好之后发送到国内:scp -P 28391 root@172.96.209.207:/root/vgg16_reducedfc.pth ./）

bug：
运行python train.py出现bug：AttributeError: 'NoneType' object has no attribute 'shape'
用中间输出发现只是部分图片读不到数据，所以排除路径的问题。应该是caffe-ssd数据的问题，所以重新下载数据//解决了上述bug

bug：
TypeError: sum() got an unexpected keyword argument 'keepdim'

使用conda list | grep pytorch  或者import torch,torch__version__可以查看pytorch
conda install pytorch torchvision -c pytorch可以更新最新版的pytorch


训练：python train.py

测试：python eval.py --trained_model weights/v2.pth

可视化：用jupyter打开/media/chencheng/data/workspace/ssd.pytorch/demo/demo.ipynb，里面有输入图片、模型路径的地方。

//我在公司训练的人车模型会把人脸照检测成车，而这个模型却不会。why？？？



训练自己的数据,动物检测为例:
首先将数据做成VOC格式,例如 /home/chencheng/data/animals/VOC2007

写一个可执行文件:/media/chencheng/data/workspace/ssd.pytorch/jobs/animals/train.sh

修改/home/chencheng/workspace/ssd.pytorch/data/voc0712.py中的类别信息,注意,无论xml里的类别是大写还是小写,这里都要用小写.
有必要的话,可以:cp train.py train_animals.py,这样便于修改为其它训练修改不同的超参数
~/workspace/ssd.pytorch/data/scripts里也有一些关于ssd的超参数可以设置.


