参照https://github.com/rdcolema/pytorch-image-classification

位置:/home/chencheng/workspace/pytorch-image-classification

将各个类别按照文件名放置即可

训练代码train.py最后三行分别表示训练\验证\测试

注意的地方:
每张图经过全连接层会得到cls_num个值,代表属于各个类别的预测分数,是按照类别的字典序排列的

第一次训练的时候会自动下载pretrain_model到~/.torch/models下

num_workers=4,##num of subprocess when load data, by default, num_workers=0, means single process 


bug:
执行python train.py时可能会出现cuda相关的报错,这是内部异步的bug,执行如下命令即可:
CUDA_LAUNCH_BLOCKING="" python train.py
