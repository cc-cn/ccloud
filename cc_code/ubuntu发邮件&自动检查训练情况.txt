


##检查训练log是否有更新##
goal_file=/home/chencheng/workspace/caffe/jobs/person_car/vgg_3over8_with_pretrain/vgg_3over8_carperson_SSD_768x432.log

while [ 1 ]
do
line_num1=`wc -l $goal_file`
echo $line_num1
sleep 2000 #每2000s 做一次检查
line_num2=`wc -l $goal_file`
echo $line_num2

if [ "${line_num1%' '*}" -eq "${line_num2%' '*}" ]
then
echo danger!!danger!!
##在这里进行发送邮件的命令

fi
done


命令行发邮件笔记：
参见 命令行发邮件_配置
脚本位置:/media/chencheng/data/tools/check_log.sh


//还有一种办法可以监测训练情况,就是将训练log放到网盘上,自动同步到手机端.
通过在mac和ubuntu上配置dropbox做到了这一点!
但这种方法不如发信息实时

